var i;

function randomiser() {
    return Math.random() * 6;
}

function showToRemove() {
    if (myNumber(i) == randomiser()) {
        makePartShake(i);
    }
}

class Map {
    remove(i) {
    }
}